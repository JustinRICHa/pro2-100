# pro2-100
A simple command-line tool for storing and retrieving user records (name, age, email) using SQLite. Supports adding and searching users, with update/delete planned.

UPDATE: added update/delete/addmany, which allow for updating an existing email via user ID, deleting via user ID, and adding multiple hard-coded users. Also added a while loop for the program to remain open unless prompted to close 
